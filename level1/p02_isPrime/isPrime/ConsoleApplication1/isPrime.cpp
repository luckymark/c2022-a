#include <stdio.h>
#include <math.h>
void main()
{
	int a, b, sum.c d;
		double e;
		scanf_s("%d %d", &a, &b);
		sum = a + b;
		c = a - b;
		d = a * b;
		e = a / b;
		printf("sum=%d,c=%d,d=%d,e=%.2f\n", sum, c, d, e);
}
